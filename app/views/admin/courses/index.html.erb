<%= link_to t("create"), new_admin_course_path,
  class: "btn btn-lg btn-primary" %><br />
<div id="paginator">
  <%= paginate @courses, remote: true %>
</div>
<div id="courses">
  <ul class="users">
    <%= render @courses %>
  </ul>
</div>
